language: python
python:
  - "2.7"
  - "3.6"
# command to install dependencies
install: 
  - pip install --upgrade pip
  - pip install wheel
  - pip install numpy scipy
  - pip install scikit-learn
  - pip install matplotlib
# command to run tests
script: travis_wait 20 nosetests
